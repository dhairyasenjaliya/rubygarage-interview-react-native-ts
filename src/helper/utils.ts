/**
 * Returns the length of a movie list.
 * @param {number} length - The length of the movie list.
 * @returns {number} - The length of the movie list.
 */
const getMovieListLength = (length: number) => {
  return length ? 1 : 0;
};

export {getMovieListLength};
